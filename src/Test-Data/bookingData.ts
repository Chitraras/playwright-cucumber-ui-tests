export const bookingData = {
    bookingIframeSelector: 'iframe[src*="calendly"]',
    bookingNameInput: '//input[contains(@placeholder, "Full name") or contains(@placeholder, "Your name") or contains(@name, "name")]',
    bookingEmailInput: '//input[contains(@placeholder, "Email") or contains(@name, "email")]',
    bookingPhoneInput: '//input[contains(@placeholder, "Phone") or contains(@name, "phone") or contains(@name, "mobile")]',
    bookingTimezoneSelect: '//select[contains(@name, "timezone") or contains(@id, "timezone")]',
    bookingSubmitButton: '//button[contains(., "Continue") or contains(., "Confirm") or contains(., "Book")]',
    otpVerificationButton: '//button[contains(., "Verify") or contains(., "Verify via SMS") or contains(., "Send OTP")]',
    bookingGenderSelect: '//select[contains(@name, "gender") or contains(@id, "gender")]',
    bookingPronounsSelect: '//select[contains(@name, "pronoun") or contains(@id, "pronoun")]',
    bookingReferralSelect: '//select[contains(@name, "referral") or contains(@id, "referral")]',
    bookingLanguageInput: '//input[contains(@name, "language") or contains(@placeholder, "language") or contains(@id, "language")]',
    bookingConsultationDetailsTextarea: '//textarea[contains(@name, "reason") or contains(@name, "detail") or contains(@name, "concern") or contains(@placeholder, "reason") or contains(@placeholder, "concern") or contains(@placeholder, "why")]',
    bookingConsentCheckbox: '//input[@type="checkbox" and (contains(@name, "consent") or contains(@id, "consent"))]',
    bookingTermsCheckbox: '//input[@type="checkbox" and (contains(@name, "terms") or contains(@id, "terms"))]',
    sessionSlotOptions: "//button[contains(@class,'border-primary-200') and (contains(normalize-space(.),'AM') or contains(normalize-space(.),'PM'))]",
    calendarBookingCTA: 'button:has-text("Book My Session — ")',
    bookingTestName: 'Automation Tester',
    bookingTestEmail: 'automation.tester@example.com',
    bookingTestPhone: '+911234567890',
    bookingTestGender: 'Female',
    bookingTestPronouns: 'She/Her',
    bookingTestReferral: 'other',
    bookingTestLanguage: 'English',
    bookingTestConsultationDetails: 'I am looking for support with stress and anxiety. I prefer morning sessions.',
    bookingTestConsent: true,
    bookingTestAgreeTerms: true,
};